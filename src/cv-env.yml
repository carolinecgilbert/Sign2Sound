name: cv-env  # Specify the name of the environment
channels:
  - defaults
dependencies:
  - python=3.8  # Specify the desired Python version
  - numpy=1.21.2
  - pandas=1.3.3
  - scikit-learn=0.24.2
  - matplotlib=3.4.3
  - opencv=4.6.0